import HeroCarousel from "./HeroCarousel";

export default function Hero() {
  return (
    <section id="home" className="relative min-h-svh supports-[height:100dvh]:min-h-[100dvh]">
      <HeroCarousel />
    </section>
  );
}
